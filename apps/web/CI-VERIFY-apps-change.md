# CI verification: apps tree change

Throwaway file under the `apps/` tree to verify that a PR touching this
tree triggers the full CI matrix (lint, type-check, test, build) as
before. Safe to delete.

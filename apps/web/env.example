# URL base de la API. Si no está definida, usa http://localhost:8080/api por defecto.
# Durante el despliegue, deploy-web.sh la resuelve automáticamente desde los outputs del stack.
VITE_API_BASE_URL=http://localhost:8080/api

## REMOVED

This spec has been removed. The yearly emissions chart endpoint is no longer required because the "Distribución por Alcance" card now always shows the category pie chart (grouped by methodology) regardless of whether a year filter is active or not.

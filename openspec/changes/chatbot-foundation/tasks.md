## 1. Database Schema (Block A)

- [x] 1.1 Add Prisma models in `packages/database/src/prisma/schema.prisma`: `ChatbotChatConversation` (mapped to `chatbot_chat_conversation`), `ChatbotChatMessage` (mapped to `chatbot_chat_message`), `ChatbotCorpusSource` (mapped to `chatbot_corpus_source`), `ChatbotCorpusChunk` (mapped to `chatbot_corpus_chunk`), `ChatbotCorpusIngestRun` (mapped to `chatbot_corpus_ingest_run`). Do NOT enable the `multiSchema` preview feature.
- [x] 1.2 Add Prisma enum `ChatMessageRole` (mapped to `chatbot_chat_message_role`) with values `USER`, `ASSISTANT`, `SYSTEM`, `TOOL`. Add Prisma enums `CorpusSourceType` (`PDF`/`MD`/`URL`/`XLSX`), `CorpusSourceScope` (`GLOBAL`/`NATIONAL`), `CorpusSourceStatus` with `snake_case` mappings prefixed with `chatbot_`.
- [x] 1.3 On `ChatbotChatConversation`, declare nullable `userId` FK to `User`, nullable `sessionId` (TEXT), nullable `organizationId` FK to `Organization`, nullable `ipHash` (TEXT), `expiresAt` (DateTime, NOT nullable), `createdAt`/`lastMessageAt`. The `userId` and `organizationId` `@relation` annotations SHALL be declared nullable AND SHALL include explicit `onDelete: SetNull`. Do NOT rely on Prisma's default referential action for nullable relations — defaults have shifted across Prisma major versions (Cascade in 2.x, SetNull from 3.x onward) and the schema must be self-contained against that drift. Match the existing repo pattern (e.g., `country_parameter.created_by_id`).
- [x] 1.4 On `ChatbotChatMessage`, declare `conversationId` (FK with `onDelete: Cascade`), `role` (ChatMessageRole), `content` (TEXT), nullable `tokensUsed` (Int), nullable `latencyMs` (Int), `truncated` (Boolean default `false`), `sourcesCited` (Json default `[]`), `createdAt`.
- [x] 1.5 On `ChatbotCorpusChunk`, declare `sourceId` (FK with `onDelete: Cascade`), `chunkIndex` (Int), `content` (TEXT), nullable `pageNumber` (Int), nullable `sectionTitle` (TEXT), nullable `tokens` (Int). **Do NOT add an `embedding` column.** That arrives in `chatbot-rag-ingest` (V1) together with the `pgvector` extension. On `ChatbotCorpusIngestRun`, declare **nullable** `sourceId` whose Prisma `@relation` annotation to `ChatbotCorpusSource` is nullable AND explicitly carries `onDelete: SetNull` (do NOT rely on Prisma's default for nullable relations — see task 1.3 for the version-drift rationale), plus `startedAt` (DateTime, default `now()`), nullable `completedAt`, nullable `chunksCreated` (Int), nullable `embeddingModel` (TEXT), nullable `triggeredBy` (TEXT). The nullable FK with explicit `SetNull` ensures audit-trail rows persist after a referenced source is deleted.
- [x] 1.6 Generate the migration via `pnpm --filter @repo/database dev:migrate -- --name add_chatbot_tables` and inspect the SQL to confirm: (a) all five tables in `public` schema with the `chatbot_` prefix; (b) CHECK constraint on `chatbot_chat_conversation` declared exactly as `CHECK ((user_id IS NOT NULL AND session_id IS NULL) OR (user_id IS NULL AND session_id IS NOT NULL) OR (user_id IS NULL AND session_id IS NULL))` — the relaxed form that tolerates the post-deletion `(NULL, NULL)` state produced by `ON DELETE SET NULL` on `user_id`. The exactly-one-of invariant on INSERT is enforced by application code, not the database; (c) FKs `chatbot_chat_conversation.user_id` and `chatbot_chat_conversation.organization_id` declare `ON DELETE SET NULL`; (d) FKs `chatbot_chat_message.conversation_id` and `chatbot_corpus_chunk.source_id` declare `ON DELETE CASCADE`; (e) FK `chatbot_corpus_ingest_run.source_id` declares `ON DELETE SET NULL` on a nullable column, preserving the audit-trail row after a referenced source is deleted; (f) indexes `(session_id, created_at DESC)`, `(user_id, expires_at)`, `(expires_at)`, `(ip_hash, created_at DESC)` on conversation, `(conversation_id, created_at)` on message; (g) `sources_cited` defaults to `'[]'::jsonb`; (h) **no `CREATE EXTENSION` statement appears**.
- [x] 1.7 Verify enums propagate: import `ChatMessageRole` from `@repo/database/enums` in a throw-away script and confirm TypeScript autocomplete exposes the four values.

## 2. Types & Schemas (Block B)

- [x] 2.1 Create `packages/types/src/chatbot/sendMessage/schemas.ts` with Zod schemas for: request body (`content` string, max length aligned to `CHATBOT_MAX_USER_INPUT_TOKENS * 4` characters), the SSE event payloads (`delta` and `error` shapes), and the error response schema reused for 400/413/500/503.
- [x] 2.2 Create `packages/types/src/chatbot/deleteMyConversation/schemas.ts` with Zod schemas for the (empty) request and the (empty) 204 response. Reuse `ApiErrorResponseSchema` for 500.
- [x] 2.3 Create `packages/types/src/chatbot/sendMessage/types.ts` and `packages/types/src/chatbot/deleteMyConversation/types.ts` exporting inferred types from the schemas.
- [x] 2.4 Create `packages/types/src/chatbot/index.ts` re-exporting both endpoint folders, and add `export * from "./chatbot/index.js"` to `packages/types/src/index.ts`.

## 3. API — Configuration & Constants (Block B)

- [x] 3.1 Append the six chatbot constants to `apps/api/src/config/constants.ts`: `CHATBOT_MAX_USER_INPUT_TOKENS = 4000`, `CHATBOT_MAX_HISTORY_TOKENS = 8000`, `CHATBOT_MAX_RAG_CONTEXT_TOKENS = 12000`, `CHATBOT_MAX_OUTPUT_TOKENS = 1500`, `CHATBOT_MAX_TURNS_PER_CONVERSATION = 50`, `CHATBOT_CONVERSATION_TTL_DAYS = 30`. Each with a one-line JSDoc.
- [x] 3.2 Add `LLM_PROVIDER` parsing to `apps/api/src/config/environment.ts` using the same whitelist + IIFE-throw pattern as `AUTH_PROVIDER`. Allowed values: `"mock"`, `"azure-openai"`. Also add a boot-time guard that throws if `LLM_PROVIDER === "mock"` AND `IS_PROD` is `true`.
- [x] 3.3 Add `COOKIE_SECRET` to `apps/api/src/config/environment.ts`. Required (throw at boot) when `IS_PROD`. Falls back to a documented dev literal locally.
- [x] 3.4 Add Azure OpenAI configuration envs to `apps/api/src/config/environment.ts`: `AZURE_OPENAI_ENDPOINT` (URL) and `AZURE_OPENAI_DEPLOYMENT_NAME` (string). Required only when `LLM_PROVIDER=azure-openai`; otherwise unused.
- [x] 3.5 Create `apps/api/src/features/chatbot/constants.ts` exporting `export const CHATBOT_GENERIC_ERROR_MESSAGE = "El asistente no está disponible en este momento. Por favor intenta nuevamente.";`. Place at the chatbot feature root (not inside any capability sub-folder) so both `sendMessage/handler.ts` (which surfaces it on 503 responses and on the mid-stream `event: error` payload) and any future capability that needs the same generic copy can import it without crossing capability boundaries. The streaming handler and tests SHALL reference this constant by name; no module SHALL transcribe the literal Spanish string.

## 4. API — Errors & Plugins (Block B)

- [x] 4.1 Create `apps/api/src/errors/ExternalServiceError.ts` using `createError("EXTERNAL_SERVICE_ERROR", "%s", 503)`.
- [x] 4.2 Create `apps/api/src/errors/RequestTooLargeError.ts` using `createError("REQUEST_TOO_LARGE", "%s", 413)`.
- [x] 4.3 Add re-exports for both new errors to `apps/api/src/errors/index.ts`.
- [x] 4.4 Install `@fastify/cookie` in `apps/api/package.json` (`pnpm --filter api add @fastify/cookie`).
- [x] 4.5 Create `apps/api/src/plugins/external/cookie.ts` registering `@fastify/cookie` with `secret: COOKIE_SECRET` and `parseOptions: { signed: true }`. Follow the file-and-`autoConfig` pattern of `cors.ts` so the autoload picks it up.

## 5. API — LLMProvider Abstraction (Block B)

- [x] 5.1 Create `apps/api/src/features/chatbot/llmProvider/types.ts` declaring the `LLMProvider` interface with `streamCompletion(messages: LlmMessage[], options: { maxOutputTokens: number; signal?: AbortSignal }): AsyncIterable<LlmStreamEvent>`. Define `LlmMessage = { role: ChatMessageRole; content: string }` and `LlmStreamEvent = { type: "delta"; content: string } | { type: "usage"; inputTokens: number; outputTokens: number }`.
- [x] 5.2 Create `apps/api/src/features/chatbot/llmProvider/estimateTokens.ts` exporting `export const estimateTokens = (text: string): number => Math.ceil(text.length / 4);`. This is the single source of truth for token estimation in the chatbot feature.
- [x] 5.3 Create `apps/api/src/features/chatbot/llmProvider/mock.ts` exporting a `LLMProvider` value that yields `"Recibí: {user_message}. Esta es una respuesta de mock."` chunked into at least three deltas, then a final `usage` event computed via `estimateTokens(joinedInput)` / `estimateTokens(output)`. Honor `options.signal` (stop iteration on abort). No imports from `openai`, `node:https`, `node:fetch`, or any network module.
- [x] 5.4 Install the Azure OpenAI SDK (`pnpm --filter api add openai`).
- [x] 5.5 Create `apps/api/src/features/chatbot/llmProvider/azureOpenAI.ts` exporting a `LLMProvider` value that uses the `openai` package's `AzureOpenAI` client constructed with `DefaultAzureCredential` from `@azure/identity` (managed-identity auth, no API keys). Read endpoint and deployment name from `AZURE_OPENAI_ENDPOINT` / `AZURE_OPENAI_DEPLOYMENT_NAME`. Translate upstream chunks into `LlmStreamEvent` shape. Pass `options.signal` to the SDK call.
- [x] 5.6 Create `apps/api/src/features/chatbot/llmProvider/index.ts` exporting `getLlmProvider()` that returns `mock` or `azureOpenAI` based on `LLM_PROVIDER`. Cache the instance — selection happens once at first call (or pre-warm at boot).
- [x] 5.7 Add a custom ESLint rule `chatbot/no-network-imports-in-mock` declared **inline in `apps/api/eslint.config.ts`** using ESLint's plugin-as-object form (no new workspace package). The rule SHALL fail when the file `apps/api/src/features/chatbot/llmProvider/mock.ts` imports any of the modules `openai`, `node:https`, `node:fetch`, `https`, `node-fetch`, or `axios`, directly or via re-export. Configure the rule with `error` severity in the `apps/api` ESLint config and scope it to that one file path.
- [x] 5.8 Add a custom ESLint rule `chatbot/single-source-estimate-tokens` declared inline in `apps/api/eslint.config.ts`. The rule SHALL fail when any file under `apps/api/src/features/chatbot/` other than `apps/api/src/features/chatbot/llmProvider/estimateTokens.ts` contains the AST pattern equivalent to `Math.ceil(<expression>.length / 4)`. Severity `error`. The rule SHALL match the pattern at the AST level (BinaryExpression `/` with right-hand 4, wrapped in `Math.ceil(...)`, applied to a `MemberExpression` whose property is `length`) so renamed identifiers and whitespace variations are still caught.

## 6. API — Identity & Cookie Helpers (Block B)

- [x] 6.1 Create `apps/api/src/features/chatbot/helpers/identity.ts` with: (a) the `ChatbotIdentity` discriminated union (`{ kind: "user"; userId: bigint } | { kind: "session"; sessionId: string }`); (b) the `chatbotIdentityPreHandler({ requireIdentity }: { requireIdentity: boolean })` factory returning a Fastify preHandler. The preHandler reads `request.currentUser` first, then a signed `chatbot_session_id` cookie. If neither is present and `requireIdentity` is true, generate UUID v4, set the signed cookie via `reply.setCookie("chatbot_session_id", id, { signed: true, httpOnly: true, sameSite: "lax", secure: IS_PROD, path: "/api/chatbot", maxAge: 60 * 60 * 24 * 30 })`, populate `request.chatbotIdentity = { kind: "session", sessionId: id }`. If `requireIdentity` is false, leave `request.chatbotIdentity` undefined when no identity resolves. NEVER respond with 401.
- [x] 6.2 Add a TypeScript module declaration extending `FastifyRequest` with the optional `chatbotIdentity?: ChatbotIdentity` decoration in `apps/api/src/types/fastify.ts` (or a new ambient declaration file under `apps/api/src/features/chatbot/helpers/`).
- [x] 6.3 Add a helper `apps/api/src/features/chatbot/helpers/clearSessionCookie.ts` that emits a `Set-Cookie` header clearing `chatbot_session_id` (`maxAge: 0`, same `path`/`httpOnly`/`sameSite`/`secure`). Used by the deletion endpoint.

## 7. API — Send Message Endpoint (Block B)

- [x] 7.1 Create `apps/api/src/features/chatbot/sendMessage/route.ts` registering `POST /message` (relative to the `/api/chatbot` scope mounted by `apps/api/src/routes/api/chatbot/index.ts`, so the canonical URL is `POST /api/chatbot/message`) with the request body schema, response schemas for `200`/`400`/`413`/`500`/`503`, and the `chatbotIdentityPreHandler({ requireIdentity: true })` as `preHandler`.
- [x] 7.2 Create `apps/api/src/features/chatbot/sendMessage/handler.ts` orchestrating: (a) resolve identity (already done by the preHandler); (b) validate caps using `estimateTokens` against user input + history, plus the turn count cap — these reads do NOT need the transaction below since they are gated on identity, not on conversation creation; (c) inside `prisma.$transaction(async (tx) => { ... })` (see task 7.6): lazy-create the conversation if no active row exists for the identity, insert the user message row with `tokens_used = NULL` and `latency_ms = NULL`, insert the assistant row with empty `content`, `tokens_used = NULL`, and `latency_ms = NULL`, capturing its `id`; (d) commit the transaction; (e) set SSE headers and `reply.hijack()`; (f) iterate `provider.streamCompletion(...)`, writing `data:` events with stable IDs (use the assistant row's id) for each `delta`, accumulating content; (g) on terminal `usage` event, emit the `event: done` SSE event with `{"inputTokens":N,"outputTokens":M}` payload, then finalize the assistant row OUTSIDE the transaction with the accumulated content, `tokens_used = inputTokens + outputTokens`, `latency_ms`, and `truncated = false`; (h) install a `reply.raw.on('close')` finalizer that emits `UPDATE chatbot_chat_message SET truncated = true, content = $partial WHERE id = $assistantRowId AND latency_ms IS NULL`. The success path setting `latency_ms` makes the disconnect UPDATE a no-op when both fire concurrently — idempotency is provided by the conditional WHERE, not by an in-process flag. The invariant `latency_ms IS NOT NULL` ⟺ assistant row finalized successfully holds because no other code path ever sets `latency_ms`; (i) on pre-stream provider error, throw `ExternalServiceError`; on mid-stream provider error, write the documented terminal SSE error event (`event: error\ndata: {"code":"EXTERNAL_SERVICE_ERROR","message":"..."}\n\n`) and end the response.
- [x] 7.3 Create `apps/api/src/features/chatbot/sendMessage/service.ts` with helpers: `resolveOrCreateConversation(prisma, identity)` (lazy-create with `expires_at`); `loadConversationHistory(prisma, conversationId, limit)`; `enforceTokenCaps(userContent, historyContent)` (throws `RequestTooLargeError`); `enforceTurnCap(prisma, conversationId)` (throws `RequestTooLargeError`).
- [x] 7.4 Create `apps/api/src/features/chatbot/sendMessage/helpers.ts` with the `writeSseEvent(reply, event, data)` helper that formats SSE wire frames consistently (named events, JSON `data:` payloads, blank-line terminators).
- [x] 7.5 Set the SSE headers explicitly in `route.ts` or `handler.ts`: `Content-Type: text/event-stream`, `Cache-Control: no-cache, no-transform`, `X-Accel-Buffering: no`, `Connection: keep-alive`.
- [x] 7.6 Wrap lazy-create + user message insert + empty assistant insert in a single `prisma.$transaction(async (tx) => { ... })` (interactive form). As the FIRST statement inside the transaction, BEFORE any read or write that touches `chatbot_chat_conversation`, execute `SELECT pg_advisory_xact_lock(hashtextextended($key, 0))` via `tx.$executeRaw` (or `tx.$queryRaw` — both share the transaction client) where `$key` is the literal string `'chatbot:user:' || userId` for authenticated callers and `'chatbot:session:' || sessionId` for anonymous callers. This advisory lock — not the transaction alone — is what closes the TOCTOU race for concurrent first-message turns: under PostgreSQL's default `READ COMMITTED` isolation, two concurrent transactions can each see "no active conversation" and each insert one; the identity-scoped advisory lock serializes them at the database level. The lock auto-releases on commit or rollback. The transaction SHALL close BEFORE invoking `provider.streamCompletion(...)` — keeping the transaction (and its advisory lock) open for the duration of an LLM stream is not acceptable. The assistant row finalize at stream end (with content, `tokens_used`, `latency_ms`, `truncated = false`) and the disconnect finalizer (`truncated = true`) SHALL execute as standalone Prisma writes outside that transaction. Add a code comment near the lock acquisition explaining the TOCTOU race it closes (concurrent first-message requests for the same identity) and noting that removing the lock without an alternative serialization mechanism (e.g., a unique partial index plus `INSERT ... ON CONFLICT`) would re-introduce the race.

## 8. API — Delete My Conversation Endpoint (Block B)

- [x] 8.1 Create `apps/api/src/features/chatbot/deleteMyConversation/route.ts` registering `DELETE /conversations/me` with response schemas for `204`/`500` and `chatbotIdentityPreHandler({ requireIdentity: false })` as `preHandler`.
- [x] 8.2 Create `apps/api/src/features/chatbot/deleteMyConversation/handler.ts`: (a) if `request.chatbotIdentity?.kind === "user"`, delete by `user_id`; (b) if `kind === "session"`, delete by `session_id` AND emit cookie-clearing header via `clearSessionCookie(reply)`; (c) if `request.chatbotIdentity` is undefined, do nothing and respond 204 (no `Set-Cookie`). Always respond 204.
- [x] 8.3 Create `apps/api/src/features/chatbot/deleteMyConversation/service.ts` with `deleteConversationsForUser(prisma, userId)` and `deleteConversationsForSession(prisma, sessionId)` using `prisma.chatbotChatConversation.deleteMany(...)`. Cascade handles message rows.

## 9. API — Routing & Tests (Block B)

- [x] 9.1 Create `apps/api/src/routes/api/chatbot/index.ts` registering both `sendMessageRoute` and `deleteMyConversationRoute` (no group-level auth hooks — auth is opt-in via the local preHandler).
- [x] 9.2 Create `apps/api/test/helpers/sse.ts` exporting `collectSseEvents(app, url, body, options?: { headers?, cookies?, timeoutMs? })` that calls `await app.listen({ port: 0 })`, hits the URL with `fetch`, consumes the `ReadableStream` until close, parses SSE frames into `{ id?, event?, data: any }[]`, and returns the parsed events. Implementation requirements: (a) wrap the entire listen-plus-fetch flow in `try { … } finally { await app.close(); }` so the Fastify instance is always torn down — including under failure paths — and never leaks across tests; (b) accept a `timeoutMs` parameter (default 5000) and pass an `AbortController`'s `signal` to `fetch`; on timeout, abort the stream, await `app.close()` in the same `finally`, and throw a clear `TimeoutError`; (c) before returning, release the stream reader (`reader.releaseLock()` / `reader.cancel()` as appropriate) and remove any registered listeners so the next test starts with a clean event loop.
- [x] 9.3 Create `apps/api/test/features/chatbot/sendMessage/integration.test.ts` covering: anonymous happy path (eco mock streams ≥3 `delta` events + a terminal `event: done` with `{inputTokens, outputTokens}` payload); authenticated happy path; lazy conversation creation on first message; reuse of active conversation on second message; CHECK constraint blocks both-cols / neither-cols inserts; oversized user input → 413; oversized history → 413; turn cap → 413; assistant row exists BEFORE first chunk (insert-temporal); user message row has `tokens_used = NULL` regardless of outcome; assistant row on success has `tokens_used = inputTokens + outputTokens` (assert via computed value from the same `estimateTokens` helper for the mock, NOT a hardcoded integer literal — to avoid drift if the mock copy changes); mid-stream client abort → `truncated = true` with partial content; mid-stream provider error → terminal SSE error event + `truncated = true`; pre-stream provider error → 503 with `EXTERNAL_SERVICE_ERROR`; SSE response headers present; tampered cookie → fresh session generated; conversation lifecycle "active at turn start" (turn started before `expires_at`, completed after); concurrent first-message requests for the same identity (TOCTOU coverage). The TOCTOU scenario SHALL fire `N = 10` `POST /api/chatbot/message` requests concurrently via `Promise.allSettled` for **a freshly-minted caller identity** — a test-generated `userId` or `sessionId` UUID for which the test asserts `SELECT COUNT(*) FROM chatbot_chat_conversation WHERE <identity column> = <identity value>` returns 0 immediately before the requests fire. This precondition is required so the post-assertion is unambiguously about deduplication and not contaminated by historical / expired rows. After all 10 requests settle, the test SHALL assert (a) the same `SELECT COUNT(*)` returns **exactly 1** — this is the load-bearing deduplication invariant — and (b) under stable test infrastructure all 10 requests terminate with HTTP 200 plus a complete SSE stream. The identity-scoped `pg_advisory_xact_lock` deterministically serializes concurrent first-message turns at the database level, so no transaction needs to retry under stable conditions. If the test environment produces transient infrastructure failures (DB connection drops, client cancellations, timeouts), individual requests MAY return non-200 outcomes; the test SHALL still pass as long as the deduplication invariant — zero-or-one new conversation row, never two — holds. The test SHALL also include a separate scenario for **distinct identities** (two concurrent first-message requests with different `userId`s/`sessionId`s) asserting that both transactions complete without blocking each other, evidencing that the advisory lock is identity-scoped and not global.
- [x] 9.4 Create `apps/api/test/features/chatbot/deleteMyConversation/integration.test.ts` covering: authenticated deletion by `user_id`; anonymous deletion by `session_id` with cookie cleared; idempotent 204 with no rows; no-identity caller → 204 with no `Set-Cookie`; tampered cookie → 204 with no `Set-Cookie`; auth caller's anonymous-era conversations not deleted; cascade removes message rows.
- [x] 9.5 Create `apps/api/test/features/chatbot/llmProvider/unit.test.ts` covering: `getLlmProvider()` returns mock for `LLM_PROVIDER=mock`; returns Azure for `LLM_PROVIDER=azure-openai`; throws on `LLM_PROVIDER=banana`; throws on mock-in-prod (`LLM_PROVIDER=mock` + `NODE_ENV=production`); `estimateTokens("")` returns 0; `estimateTokens("hola mundo")` returns 3; mock yields ≥3 deltas plus usage; mock honors `AbortSignal`; mock has no `openai`/`https`/`fetch` imports (verified via static check or TypeScript module reference).
- [x] 9.6 Set `LLM_PROVIDER=mock` in the test global setup (`apps/api/test/setup/globalSetup.ts` or vitest config), alongside `AUTH_PROVIDER=forced-user`. Ensure `COOKIE_SECRET` is set to a stable test value.
- [x] 9.7 Create `apps/api/test/helpers/auth.ts` exporting `withAnonymousIdentity(app, fn)` that, for the duration of `fn`, makes the chatbot's identity preHandler observe `request.currentUser = undefined` so anonymous-path scenarios are testable while the global `AUTH_PROVIDER=forced-user` setup remains unchanged. Implementation: the helper SHALL inject a per-invocation **header marker** (e.g., `x-test-anonymous-override: <random-token>`) on every request issued inside `fn`. A one-shot Fastify `preHandler` SHALL be registered that, **only when the request carries the matching token in that header**, deletes `request.currentUser` BEFORE the chatbot identity preHandler runs. This eliminates global state mutation and guarantees test isolation under concurrency: the token rotates per `withAnonymousIdentity` invocation, so two concurrent helpers with different tokens never cross-contaminate. Use this helper in every anonymous-path scenario in tasks 9.3 and 9.4.
- [x] 9.8 Create `apps/api/test/features/chatbot/lint/noWritesToDormantColumns.test.ts` (Vitest). The test SHALL execute `grep -rE '(organization_id|ip_hash|organizationId|ipHash)\s*[:=]' apps/api/src/features/chatbot/` from the repo root (via `child_process.execSync` or equivalent) and SHALL fail if any match is returned. The grep target excludes spec/test files. The pattern covers both raw SQL column names (snake_case `organization_id`, `ip_hash`) and Prisma Client field names (camelCase `organizationId`, `ipHash`) — Huella uses the Prisma Client API, so a snake_case-only check would silently miss real writes. Purpose: enforce that no foundation handler, service, or helper writes to the dormant `organization_id` or `ip_hash` columns of `chatbot_chat_conversation` — those are reserved for V1+ and the `chatbot-conversation-persistence` spec asserts they ship NULL.
- [x] 9.9 Create `apps/api/test/features/chatbot/lint/noReferencesToCorpusTables.test.ts` (Vitest). The test SHALL execute `grep -rE --include='*.ts' '(chatbotCorpusSource|chatbotCorpusChunk|chatbotCorpusIngestRun|chatbot_corpus_source|chatbot_corpus_chunk|chatbot_corpus_ingest_run)' apps/api/src/` from the repo root and SHALL fail if any match is returned. The `--include='*.ts'` flag scopes the scan to TypeScript source under `apps/api/src/` — test files (under `apps/api/test/`) and migration SQL (under `packages/database/`) are intentionally outside this path so the schema/migration assertions and the lint test itself never trip the rule. Purpose: enforce that the three corpus tables remain dormant in foundation per `chatbot-corpus-schema`. The grep covers both the Prisma model accessor names (camelCase) and the raw SQL table names (snake_case).

## 10. Frontend — Widget (Block C)

> Visual reference: `openspec/files/chatbot-foundation-widget-icon.png` (collapsed entry-point button) and `openspec/files/chatbot-foundation-widget-panel.png` (expanded chat panel). These are minimum-viable reference; the design review owns final treatment.

- [x] 10.1 Create `apps/web/src/components/Chatbot/ChatbotWidget.tsx` rendering: (a) a collapsed floating button anchored bottom-right; (b) on click, an expanded panel with a header, a message list area, and an input row with send button. State management via `useChatStream` (next task). Stable test handles via `data-testid="chatbot-widget"` plus a `data-state="<canonical state>"` attribute reflecting the six states (`empty`/`loading`/`streaming`/`error`/`truncated`/`degraded`).
- [x] 10.2 Create `apps/web/src/components/Chatbot/useChatStream.ts` exposing `{ state, messages, sendMessage, deleteHistory }`. Implementation: `sendMessage` calls `POST /api/chatbot/message` via raw `fetch` with `credentials: "include"`, reads `response.body!.getReader()`, parses SSE frames in a buffer (`event:`/`data:` lines, blank-line terminators), dispatches `delta`/`error`/`usage` events. Tracks `Last-Event-ID` from incoming events; sends it on the single retry after a transport-level failure on initial fetch. Two-strike fallback: on second consecutive transport failure when initiating a turn, sets state to `degraded`. Mid-turn disconnects (abort during stream consumption) set state to `truncated` with partial content; never retried. HTTP 413 → `error` state with input-too-large message. HTTP 503 → `error` state with temporarily-unavailable message. Other 4xx/5xx → `error` state with generic message.
- [x] 10.3 Create `apps/web/src/components/Chatbot/MessageBubble.tsx` rendering a single message. Assistant content via `react-markdown` (reuse the `remark-math` + `remark-gfm` + `rehype-katex` setup from `ExplanationContent.tsx`). User content as plain text.
- [x] 10.4 Create `apps/web/src/components/Chatbot/ChatbotIcon.tsx` rendering the collapsed entry-point button per the icon Figma reference.
- [x] 10.5 Mount `<ChatbotWidget />` in `apps/web/src/routes/__root.tsx` so it is reachable from both the public landing route (`/`) and authenticated routes. Add a comment near the mount point: `// Minimum-viable placement — design review may relocate.`
- [x] 10.6 Use existing theme tokens for colors and spacing (`theme.palette.*`, `alpha()`, MUI components). Do NOT introduce new palette entries. All user-facing copy in Spanish. NO first-open disclaimer.
- [x] 10.7 Add a `server.proxy` block to `apps/web/vite.config.ts` that proxies `/api` to `VITE_API_BASE_URL` in dev (e.g., `server: { proxy: { "/api": { target: env.VITE_API_BASE_URL, changeOrigin: true } } }`). The widget's `fetch("/api/chatbot/message")` is a relative URL by design so it stays same-origin from the browser's perspective — this is required for the `chatbot_session_id` cookie (`SameSite=Lax`) to be sent with `credentials: "include"`. The existing `apiClient` (which uses absolute URLs via `prefixUrl`) is unaffected because it relies on bearer auth, not cookies.

## 11. Documentation

- [ ] 11.1 Add a "Chatbot conversation persistence and retention" section to `docs/security/sensitive-data.md` covering: tables, scope (anonymous + authenticated), 30-day TTL via `expires_at`, pg_cron job pending, cookie security properties, `DELETE /api/chatbot/conversations/me` for right-to-be-forgotten.
- [ ] 11.2 Add `LLM_PROVIDER`, `COOKIE_SECRET`, `AZURE_OPENAI_ENDPOINT`, `AZURE_OPENAI_DEPLOYMENT_NAME` to `docs/development/environment-variables.md` under appropriate sections. Also add a one-line note in the dev-setup section: "The chatbot widget makes its requests to relative URL `/api/chatbot/...` so the `chatbot_session_id` cookie (`SameSite=Lax`) is sent. The Vite proxy added in `apps/web/vite.config.ts` (see task 10.7) forwards `/api/*` to `VITE_API_BASE_URL` in dev, keeping the widget same-origin with the API."
- [ ] 11.3 Add a "Chatbot conversation purge" subsection to `docs/operations/runbook.md` declaring the pg_cron extension + daily purge job as a deferred infra change, with the SQL the future job will execute (`DELETE FROM chatbot_chat_conversation WHERE expires_at < NOW();`).

## 12. Verification

- [ ] 12.1 Run `pnpm format --filter=api --filter=web --filter=types --filter=database`, `pnpm lint --filter=api --filter=web --filter=types --filter=database`, and `pnpm type-check` (global, since `@repo/types` changes propagate). Fix any issues introduced by this PR. **If lint or type-check fails on files outside the chatbot scope (i.e., files this PR did not touch), open a separate issue and do NOT fix in this PR — that is pre-existing technical debt and out of scope.** Lint enforces zero warnings.
- [ ] 12.2 Run `pnpm test --filter=api -- /chatbot --coverage=false` and verify all chatbot integration and unit tests pass.
- [ ] 12.3 Local end-to-end smoke: with `LLM_PROVIDER=mock` and `pnpm dev`, open the web app, click the widget, send a message, observe (a) tokens render incrementally, (b) Network tab shows `text/event-stream` response with the documented headers, (c) `chatbot_session_id` cookie set with `HttpOnly`, `SameSite=Lax`, `Path=/api/chatbot`, no `Secure` (dev), (d) database has matching `chatbot_chat_conversation` row with `expires_at` ≈ now + 30 days and two `chatbot_chat_message` rows (USER + ASSISTANT) with `tokens_used` and `latency_ms` set.
- [ ] 12.4 Local mid-stream cancel smoke: send a message, click away or close the panel before completion, confirm the assistant row has `truncated = true` and the partial content matches what was visible.
- [ ] 12.5 Local DELETE smoke: send a message, invoke the deletion affordance, confirm 204 response, the cookie-clearing `Set-Cookie` header in DevTools, and that the conversation row is gone (cascade removed messages).
- [ ] 12.6 Run `openspec validate chatbot-foundation --strict` and confirm a clean pass.

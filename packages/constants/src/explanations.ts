/**
 * Maximum number of characters accepted for an explanation's markdown content.
 *
 * TODO: surface this limit in the maintainer's edit textarea (e.g. as a helper
 * text or character counter) so operators see the cap while typing.
 */
export const EXPLANATION_CONTENT_MAX_LENGTH = 10000;

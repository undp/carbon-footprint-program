// TODO: refactor to fetch from database

/**
 * Factor sources whose factor value is entered manually by the user, instead of
 * being looked up in the methodology's factor library.
 */
export const CUSTOM_FACTOR_SOURCES = ["Otro"];

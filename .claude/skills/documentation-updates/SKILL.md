---
name: Documentation Updates
description: Keep the project documentation in the docs/ folder up to date whenever a feature is added or a significant change is made. Place docs in the right location (architecture, data-model, development, operations) following the existing structure. Use when finishing a feature or significant change that should be reflected in the docs.
when_to_use: Use after implementing a new feature or a significant change, when deciding whether and where to add or update documentation under docs/.
---

# Documentation Updates

On every new feature or significant change, update the project documentation in the `docs/` folder. Create new files or update existing ones as needed to reflect the changes made.

## Where to put it

Review the existing docs structure before writing, and place documentation in the appropriate location:

- `docs/architecture/`
- `docs/data-model/`
- `docs/development/`
- `docs/operations/`
- and any other existing top-level sections.

Match the conventions and layout of the surrounding docs rather than introducing a new structure.
